# Global Instructions

- References are read-only context by default.
- Do not modify files outside the active workspace repository unless I explicitly ask for cross-repo edits.
- Keep changes small and scoped to one repository at a time, even when other repositories are available as references.
- If a requested task appears to require edits in another repository, ask before making those edits.
